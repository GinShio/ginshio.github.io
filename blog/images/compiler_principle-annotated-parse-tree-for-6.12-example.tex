\documentclass[tikz]{standalone}
\usepackage{amsmath,mathtools}
\usetikzlibrary{positioning,calc,trees}
\begin{document}
\begin{tikzpicture}[
  level distance = 5em,
  sibling distance = 9em,
  ]
  \node{\(E.addr=t_{5}\)}
  child {
    node {\(E.addr=c\)}
    child {
      node {c}
    }
  }
  child {
    node {\(+\)}
  }
  child {
    node {\(E.addr=t_{4}\)}
    child {
      node {\(\begin{aligned}
        L.array &= a\\
        L.type  &= integer\\
        L.addr  &= t_{3}
      \end{aligned}\)}
    child {
      node {\(\begin{aligned}
        L.array &= a\\
        L.type  &= array(3, integer)\\
        L.addr  &= t_{1}
      \end{aligned}\)}
    child {
      node (todo) {\(a.type =\)}
    }
    child {
      node {\([\)}
    }
    child {
      node {\(E.addr = i\)}
      child {
        node {i}
      }
    }
    child {
      node {\(]\)}
    }
  }
  child {
    node {\([\)}
  }
  child {
    node {\(E.addr = j\)}
    child {
      node {j}
    }
  }
  child {
    node {\(]\)}
  }
}
};

\node[below=4.0ex of $(todo.south)!0.5!(todo.south west)$,anchor=south west]
{\(array(2, array(3, integer))\)};
\end{tikzpicture}
\end{document}
